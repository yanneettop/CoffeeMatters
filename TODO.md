# Gradual Hover Zoom Effect

- [x] Update `src/index.css` — Changed `.img-hover .img-content` transition to `1.2s cubic-bezier(0.25, 0.1, 0.25, 1)`
- [x] Update `src/App.css` — Fixed `img.img-content[src]` transition to explicitly list both opacity and transform
